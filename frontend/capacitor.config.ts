import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
    appId: "sh.reakt.railcall",
    appName: "RailCall",
    webDir: "dist/frontend/browser"
};

export default config;
